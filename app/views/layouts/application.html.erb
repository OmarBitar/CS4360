<!DOCTYPE html>
<html>
  <head>
    <title>Schedule</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div id='container'>
      <div id='header'>
        Really Good Pizza              <!-- this needs to be a variable -->
        <%= link_to 'Log out',destroy_user_session_path,method: :delete%>
      </div>
      <%= render "layouts/menu" %>
      <p class="notice">test 1<%= notice %></p>
      <p class="alert">test 2<%= alert %></p>
      <div id='yield'>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
