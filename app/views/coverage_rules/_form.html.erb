<%= form_with(model: coverage_rule, local: true) do |form| %>
  <% if coverage_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coverage_rule.errors.count, "error") %> prohibited this coverage_rule from being saved:</h2>

      <ul>
        <% coverage_rule.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :user_id, value: current_user.id %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
